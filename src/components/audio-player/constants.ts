export enum STREAMING_AUDIO_PLAYER_EVENT {
  RESUME = "resume",
  PLAY = "play",
  PAUSE = "pause",
}

export enum STREAMING_AUDIO_PLAYER_STATE {
  IDLE = "idle",
  PLAYING = "playing",
  PAUSED = "paused",
  PROCESSING = "processing",
}
